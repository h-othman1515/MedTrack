<button type="button"
        class="theme-toggle"
        data-mt-theme-toggle
        aria-label="@(ViewData["ThemeToggleLabel"] as string ?? "Toggle theme")"
        title="Toggle dark / light mode"></button>
